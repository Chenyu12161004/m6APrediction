% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_multiple}
\alias{prediction_multiple}
\title{Predict m6A Modification Status for Multiple Sites}
\usage{
prediction_multiple(ml_fit, feature_df, positive_threshold = 0.5)
}
\arguments{
\item{ml_fit}{A fitted random forest model object, typically loaded from an
.rds file.}

\item{feature_df}{A data frame containing the input features for prediction.
Must include columns: \code{gc_content}, \code{RNA_type}, \code{RNA_region},
\code{exon_length}, \code{distance_to_junction}, \code{evolutionary_conservation}, and
\code{DNA_5mer}.}

\item{positive_threshold}{A numeric value between 0 and 1. The probability
threshold above which a site is classified as "Positive". Defaults to 0.5.}
}
\value{
The input \code{feature_df} augmented with two new columns:
\code{predicted_m6A_prob} (the probability of being a positive m6A site) and
\code{predicted_m6A_status} ("Positive" or "Negative").
}
\description{
This function takes a data frame of input features and predicts the m6A
modification probability and status for each site using a pre-trained
random forest model.
}
\examples{
# Load the pre-trained model from the package's external data
rf_model <- readRDS(
  system.file("extdata", "rf_fit.rds", package = "m6APrediction")
)

# Load the example input data from the package
example_df <- read.csv(
  system.file("extdata", "m6A_input_example.csv", package = "m6APrediction")
)

# Run the prediction function with the loaded data
predictions <- prediction_multiple(rf_model, example_df, positive_threshold = 0.6)

# Print the first few rows of the results
head(predictions)

}
