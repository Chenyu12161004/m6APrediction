% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Predict m6A Modification Status for a Single Site}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{A fitted random forest model object.}

\item{gc_content}{Numeric value for GC content (0-1).}

\item{RNA_type}{Character string for RNA type (e.g., "mRNA").}

\item{RNA_region}{Character string for RNA region (e.g., "CDS").}

\item{exon_length}{Numeric value for exon length.}

\item{distance_to_junction}{Numeric value for distance to junction.}

\item{evolutionary_conservation}{Numeric value for evolutionary conservation (0-1).}

\item{DNA_5mer}{A 5-character string representing the DNA sequence.}

\item{positive_threshold}{A numeric value (0-1) used as the classification
threshold. Defaults to 0.5.}
}
\value{
A named character vector of length 2, containing the
\code{predicted_m6A_prob} and \code{predicted_m6A_status}.
}
\description{
This function predicts the m6A modification probability and status for a
single site based on manually provided feature values.
}
\examples{
# Load the pre-trained model from the package's external data
rf_model <- readRDS(
  system.file("extdata", "rf_fit.rds", package = "m6APrediction")
)

# Run the prediction for a single data point
single_prediction <- prediction_single(
  ml_fit = rf_model,
  gc_content = 0.5,
  RNA_type = "mRNA",
  RNA_region = "CDS",
  exon_length = 10,
  distance_to_junction = 8,
  evolutionary_conservation = 0.5,
  DNA_5mer = "GGACA"
)

# Print the result
print(single_prediction)

}
